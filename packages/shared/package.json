{
  "name": "@propai/shared",
  "private": true,
  "version": "0.1.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo 'shared build not initialized'",
    "lint": "echo 'shared lint not initialized'",
    "test": "echo 'shared test not initialized'"
  }
}
